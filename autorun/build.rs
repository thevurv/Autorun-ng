// Autorun depends on having an exe that communicates with a dll that will be injected into the Garry's Mod process.
// This dll will be built and then included as bytes in the application.

// This build file defines OUT_DIR (so is necessary despite being empty.)
fn main() {}